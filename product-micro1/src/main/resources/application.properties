# Server port for the order microservice
server.port=8081

# MongoDB Configuration (or other database)
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=orderdb

# RestTemplate configuration for inter-service communication
# (This is configured in the RestTemplateConfig.java file, but we can add
# properties here for a more centralized approach if needed)
